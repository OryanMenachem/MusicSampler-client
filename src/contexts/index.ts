import NotesProvider, { notesContext } from "./Notes.context";
import ColumnsProvider, { columnsContext } from "./Columns.context";
import ControlsPlayerProvider, {
  controlsPlayerContext,
} from "./ControlsPlayer.context";
import InstrumentProvider, { instrumentContext } from "./Instrument.context";
import ActiveColumnProvider, {
  activeColumnContext,
} from "./ActiveColumn.context";
export {
  notesContext,
  columnsContext,
  controlsPlayerContext,
  instrumentContext,
  activeColumnContext,
  NotesProvider,
  ColumnsProvider,
  ControlsPlayerProvider,
  InstrumentProvider,
  ActiveColumnProvider,
};
